<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedReminder">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.AddMedicationActivity" android:exported="false" />
        <activity android:name=".ui.AppointmentsActivity" android:exported="false" />
        <activity android:name=".ui.AddAppointmentActivity" android:exported="false" />
        <activity android:name=".ui.TasksActivity" android:exported="false" />
        <activity android:name=".ui.AddTaskActivity" android:exported="false" />

        <activity
            android:name=".alarm.AlarmRingActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.MedReminder.Alarm" />

        <receiver android:name=".alarm.MedicationAlarmReceiver" android:exported="false" />
        <receiver android:name=".alarm.TakenActionReceiver" android:exported="false" />
        <receiver android:name=".alarm.TaskAlarmReceiver" android:exported="false" />
        <receiver android:name=".alarm.AppointmentAlarmReceiver" android:exported="false" />

        <receiver android:name=".alarm.BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
